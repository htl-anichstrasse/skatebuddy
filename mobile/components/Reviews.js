import React from 'react';
import { View, Text } from 'react-native';

const Reviews = ({ skateparkId }) => {
  return (
    <View>
      <Text>{skateparkId}</Text>
    </View>
  );
};

export default Reviews;
