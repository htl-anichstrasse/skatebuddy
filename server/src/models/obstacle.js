class Obstacle {
    constructor(id, description, difficulty) {
        this.id = id;
        this.description = description;
        this.difficulty = difficulty;
    }
}

module.exports = Obstacle;
